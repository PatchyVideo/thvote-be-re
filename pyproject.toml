[project]
name = "thvote-be-re"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "alembic",
    "sqlalchemy",
    "redis",
    "aioredis",
    "redis-om",
    "fastapi",
    "pydantic",
    "strawberry-graphql[fastapi]",
    "uvicorn",
]
