apiVersion: v1
kind: Service
metadata:
  name: toho-vote-be
  labels:
    app: toho-vote-be
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: toho-vote-be
